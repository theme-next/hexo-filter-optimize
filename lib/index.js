const filter = require('./filter');
const css = require('./css');
const js = require('./js');
const image = require('./image');

// the main filter function
module.exports = { filter, css, js, image };
